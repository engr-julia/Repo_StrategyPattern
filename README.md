# Repo_StrategyPattern
This program applies the Strategy Design Pattern to manage different attack and defense behaviors of game characters. Instead of using multiple if-else statements, each character is composed of interchangeable AttackStrategy and DefenseStrategy objects.
